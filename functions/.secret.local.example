# For LOCAL EMULATOR use only: copy to functions/.secret.local (gitignored).
# For a real deployment, set these with the Firebase CLI instead — never
# commit real values:
#   firebase functions:secrets:set LIVEKIT_API_KEY
#   firebase functions:secrets:set LIVEKIT_API_SECRET

LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=
