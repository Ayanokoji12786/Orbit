import { StubScreen } from '@/components/navigation/StubScreen';

export default function EditProfile() {
  return (
    <StubScreen
      title="Edit Profile"
      icon="person-circle-outline"
      subtitle="Editing your avatar, bio, and social links needs a connected Supabase project to persist — coming in Phase 2."
    />
  );
}
