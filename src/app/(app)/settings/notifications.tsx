import { StubScreen } from '@/components/navigation/StubScreen';

export default function NotificationSettings() {
  return (
    <StubScreen
      title="Notifications"
      icon="notifications-outline"
      subtitle="Fine-grained notification controls arrive in Phase 3, once push notifications are wired up."
    />
  );
}
