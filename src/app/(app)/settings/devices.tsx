import { StubScreen } from '@/components/navigation/StubScreen';

export default function DeviceSettings() {
  return (
    <StubScreen
      title="Devices"
      icon="hardware-chip-outline"
      subtitle="Camera, microphone, and speaker selection appear here once live video calling ships in Phase 2."
    />
  );
}
