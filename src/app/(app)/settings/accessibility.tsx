import { StubScreen } from '@/components/navigation/StubScreen';

export default function AccessibilitySettings() {
  return (
    <StubScreen
      title="Accessibility"
      icon="accessibility-outline"
      subtitle="Dynamic text scaling, high contrast, and reduced motion toggles land during the Phase 5 accessibility pass."
    />
  );
}
