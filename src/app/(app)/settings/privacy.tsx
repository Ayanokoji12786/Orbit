import { StubScreen } from '@/components/navigation/StubScreen';

export default function PrivacySettings() {
  return (
    <StubScreen
      title="Privacy & Security"
      icon="lock-closed-outline"
      subtitle="Encryption options, session management, and data controls land once the Supabase backend is connected."
    />
  );
}
